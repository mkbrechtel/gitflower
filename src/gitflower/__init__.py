"""gitflower — a git-based development platform.

Local-first and git-centric: branch workflows enforced by git hooks, and a
web UI that browses hosted repositories. The web API is the frontend — every
endpoint serves JSON, a full HTML page, and an HTML fragment alike.
"""

__version__ = "0.1.0"
